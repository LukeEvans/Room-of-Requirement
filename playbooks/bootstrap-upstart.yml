---

- name: Bootstrap upstart
  hosts:
  - tag_Name_reqroom
  user: ubuntu
  sudo: true
  gather_facts: false
  tasks:
      - name: Copy all upstart configs
        copy: src=files/upstart/{{ item }} dest=/etc/init/
        with_items:
           - requirement-frontend.conf

