---

- name: Bootstrap upstart
  hosts:
  - tag_Name_reqroom-frontend-01
  user: ubuntu
  sudo: true
  gather_facts: false
  tasks:
      - name: Copy all upstart configs
        copy: src=files/upstart/{{ item }} dest=/etc/init/
        with_items:
           - reqroom-frontend.conf

